apiVersion: batch/v1
kind: Job
metadata:
  name: course-service-migration-job
spec:
  template:
    spec:
      containers:
        - name: migrator
          image: learnsphere/course-service
          command: ["node", "dist/db/migrate.js"]
          envFrom:
            - configMapRef:
                name: course-service-config
            - secretRef:
                name: auth-secrets
      restartPolicy: OnFailure
  backoffLimit: 4
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: course-service
  template:
    metadata:
      labels:
        app: course-service
    spec:
      containers:
        - name: course-service
          image: learnsphere/course-service
          ports:
            - containerPort: 8003
          readinessProbe:
            httpGet:
              path: /api/courses/health
              port: 8003
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/courses/health
              port: 8003
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          envFrom:
            - configMapRef:
                name: course-service-config
            - secretRef:
                name: auth-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: course-service-clusterip
spec:
  type: ClusterIP
  selector:
    app: course-service
  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: 8003
